start-page: p-crime-reported

p-crime-reported:
    - goto: p-crime-not-reported
      if: [==, q-crime-reported, No]
    - goto: p-date-reported
      if: [==, q-crime-reported, Yes]

p-crime-not-reported:
    - goto: end

p-date-reported:
    - goto: p-police-force
      if: [dateNotInFuture, q-date-reported]
    - goto: end

p-police-force:
    - goto: end

end: []
